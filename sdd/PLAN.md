# Plan Técnico: Implementación de la Suma

## Arquitectura propuesta
Crearemos un archivo de código y un archivo de pruebas en Node.js usando el módulo nativo `assert` para no instalar dependencias.

## Estructura de archivos
1. `src/calculadora.js`: Contendrá la función `sumar(a, b)`.
2. `tests/calculadora.test.js`: Contendrá las pruebas automatizadas.

## Control Humano
- [x] **PLAN APROBADO POR EL DESARROLLADOR** (Al marcar esta 'X', autorizas a la IA a proceder).
